# Copy this file to personal/.env.local and fill in your values.
# Then symlink to root: ln -s personal/.env.local .env.local

# NextAuth — required for authentication
# Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Perplexity API key — get one at https://www.perplexity.ai/settings/api
# Uses the Agent API (/v1/responses)
# Model options: sonar, sonar-pro, openai/gpt-4o, openai/gpt-5.2, etc.
PERPLEXITY_API_KEY=
# PERPLEXITY_MODEL=sonar-pro
PERPLEXITY_MODEL=sonar-pro
